dataset_name: Librispeech # Capitalized first letter
dataset_dir: /mnt/disks/data_disk/data/librispeech
dataset_files:           # dataset filenames to be processed, files should be in dataset_dir
    # key is dataset name, value is filename to be processed
    train: [train-clean-100, train-clean-360, train-other-500]
    dev: [dev-clean, dev-other]
    test: [test-clean, test-other]
lexicon_path: /home/dzubke/awni_speech/speech-lfs/data/lexicon/libsp-unk-lexicon_2021-02-11.txt
force_convert: False    # Converts audio even if .wav file exists
min_duration: 1         # second
max_duration: 20        # seconds
print_modulus: 40000
process_transcript: True
# max number of utterances for a single id
constraints:
